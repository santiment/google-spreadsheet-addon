/**
* Gets the daily average marketcap
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_AVG_MARKETCAP_USD (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_avg_marketcap_usd', projectSlug, from, to)
}
/**
* Gets the daily average marketcap
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_AVG_MARKETCAP_USD_1D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_avg_marketcap_usd_1d', projectSlug, from, to)
}
/**
* Gets the daily average marketcap
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_AVG_MARKETCAP_USD_7D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_avg_marketcap_usd_7d', projectSlug, from, to)
}
/**
* Gets the daily average marketcap
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_AVG_MARKETCAP_USD_30D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_avg_marketcap_usd_30d', projectSlug, from, to)
}
/**
* Gets the daily average marketcap
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_AVG_MARKETCAP_USD_60D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_avg_marketcap_usd_60d', projectSlug, from, to)
}
/**
* Gets the daily average marketcap
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_AVG_MARKETCAP_USD_90D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_avg_marketcap_usd_90d', projectSlug, from, to)
}
/**
* Gets the daily average marketcap
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_AVG_MARKETCAP_USD_180D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_avg_marketcap_usd_180d', projectSlug, from, to)
}
/**
* Gets the daily average marketcap
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_AVG_MARKETCAP_USD_365D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_avg_marketcap_usd_365d', projectSlug, from, to)
}
/**
* Gets the daily average marketcap
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_AVG_MARKETCAP_USD_2Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_avg_marketcap_usd_2y', projectSlug, from, to)
}
/**
* Gets the daily average marketcap
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_AVG_MARKETCAP_USD_3Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_avg_marketcap_usd_3y', projectSlug, from, to)
}
/**
* Gets the daily average marketcap
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_AVG_MARKETCAP_USD_5Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_avg_marketcap_usd_5y', projectSlug, from, to)
}
/**
* Gets the daily average marketcap
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_AVG_MARKETCAP_USD_10Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_avg_marketcap_usd_10y', projectSlug, from, to)
}
/**
* Gets the daily average price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_AVG_PRICE_USD (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_avg_price_usd', projectSlug, from, to)
}
/**
* Gets the daily average price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_AVG_PRICE_USD_1D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_avg_price_usd_1d', projectSlug, from, to)
}
/**
* Gets the daily average price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_AVG_PRICE_USD_7D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_avg_price_usd_7d', projectSlug, from, to)
}
/**
* Gets the daily average price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_AVG_PRICE_USD_30D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_avg_price_usd_30d', projectSlug, from, to)
}
/**
* Gets the daily average price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_AVG_PRICE_USD_60D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_avg_price_usd_60d', projectSlug, from, to)
}
/**
* Gets the daily average price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_AVG_PRICE_USD_90D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_avg_price_usd_90d', projectSlug, from, to)
}
/**
* Gets the daily average price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_AVG_PRICE_USD_180D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_avg_price_usd_180d', projectSlug, from, to)
}
/**
* Gets the daily average price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_AVG_PRICE_USD_365D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_avg_price_usd_365d', projectSlug, from, to)
}
/**
* Gets the daily average price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_AVG_PRICE_USD_2Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_avg_price_usd_2y', projectSlug, from, to)
}
/**
* Gets the daily average price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_AVG_PRICE_USD_3Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_avg_price_usd_3y', projectSlug, from, to)
}
/**
* Gets the daily average price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_AVG_PRICE_USD_5Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_avg_price_usd_5y', projectSlug, from, to)
}
/**
* Gets the daily average price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_AVG_PRICE_USD_10Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_avg_price_usd_10y', projectSlug, from, to)
}
/**
* Gets the daily closing marketcap
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_CLOSING_MARKETCAP_USD (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_closing_marketcap_usd', projectSlug, from, to)
}
/**
* Gets the daily closing marketcap
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_CLOSING_MARKETCAP_USD_1D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_closing_marketcap_usd_1d', projectSlug, from, to)
}
/**
* Gets the daily closing marketcap
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_CLOSING_MARKETCAP_USD_7D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_closing_marketcap_usd_7d', projectSlug, from, to)
}
/**
* Gets the daily closing marketcap
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_CLOSING_MARKETCAP_USD_30D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_closing_marketcap_usd_30d', projectSlug, from, to)
}
/**
* Gets the daily closing marketcap
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_CLOSING_MARKETCAP_USD_60D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_closing_marketcap_usd_60d', projectSlug, from, to)
}
/**
* Gets the daily closing marketcap
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_CLOSING_MARKETCAP_USD_90D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_closing_marketcap_usd_90d', projectSlug, from, to)
}
/**
* Gets the daily closing marketcap
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_CLOSING_MARKETCAP_USD_180D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_closing_marketcap_usd_180d', projectSlug, from, to)
}
/**
* Gets the daily closing marketcap
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_CLOSING_MARKETCAP_USD_365D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_closing_marketcap_usd_365d', projectSlug, from, to)
}
/**
* Gets the daily closing marketcap
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_CLOSING_MARKETCAP_USD_2Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_closing_marketcap_usd_2y', projectSlug, from, to)
}
/**
* Gets the daily closing marketcap
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_CLOSING_MARKETCAP_USD_3Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_closing_marketcap_usd_3y', projectSlug, from, to)
}
/**
* Gets the daily closing marketcap
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_CLOSING_MARKETCAP_USD_5Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_closing_marketcap_usd_5y', projectSlug, from, to)
}
/**
* Gets the daily closing marketcap
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_CLOSING_MARKETCAP_USD_10Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_closing_marketcap_usd_10y', projectSlug, from, to)
}
/**
* Gets the daily closing price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_CLOSING_PRICE_USD (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_closing_price_usd', projectSlug, from, to)
}
/**
* Gets the daily closing price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_CLOSING_PRICE_USD_1D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_closing_price_usd_1d', projectSlug, from, to)
}
/**
* Gets the daily closing price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_CLOSING_PRICE_USD_7D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_closing_price_usd_7d', projectSlug, from, to)
}
/**
* Gets the daily closing price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_CLOSING_PRICE_USD_30D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_closing_price_usd_30d', projectSlug, from, to)
}
/**
* Gets the daily closing price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_CLOSING_PRICE_USD_60D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_closing_price_usd_60d', projectSlug, from, to)
}
/**
* Gets the daily closing price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_CLOSING_PRICE_USD_90D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_closing_price_usd_90d', projectSlug, from, to)
}
/**
* Gets the daily closing price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_CLOSING_PRICE_USD_180D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_closing_price_usd_180d', projectSlug, from, to)
}
/**
* Gets the daily closing price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_CLOSING_PRICE_USD_365D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_closing_price_usd_365d', projectSlug, from, to)
}
/**
* Gets the daily closing price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_CLOSING_PRICE_USD_2Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_closing_price_usd_2y', projectSlug, from, to)
}
/**
* Gets the daily closing price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_CLOSING_PRICE_USD_3Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_closing_price_usd_3y', projectSlug, from, to)
}
/**
* Gets the daily closing price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_CLOSING_PRICE_USD_5Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_closing_price_usd_5y', projectSlug, from, to)
}
/**
* Gets the daily closing price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_CLOSING_PRICE_USD_10Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_closing_price_usd_10y', projectSlug, from, to)
}
/**
* Gets the daily high price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_HIGH_PRICE_USD (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_high_price_usd', projectSlug, from, to)
}
/**
* Gets the daily high price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_HIGH_PRICE_USD_1D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_high_price_usd_1d', projectSlug, from, to)
}
/**
* Gets the daily high price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_HIGH_PRICE_USD_7D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_high_price_usd_7d', projectSlug, from, to)
}
/**
* Gets the daily high price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_HIGH_PRICE_USD_30D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_high_price_usd_30d', projectSlug, from, to)
}
/**
* Gets the daily high price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_HIGH_PRICE_USD_60D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_high_price_usd_60d', projectSlug, from, to)
}
/**
* Gets the daily high price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_HIGH_PRICE_USD_90D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_high_price_usd_90d', projectSlug, from, to)
}
/**
* Gets the daily high price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_HIGH_PRICE_USD_180D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_high_price_usd_180d', projectSlug, from, to)
}
/**
* Gets the daily high price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_HIGH_PRICE_USD_365D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_high_price_usd_365d', projectSlug, from, to)
}
/**
* Gets the daily high price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_HIGH_PRICE_USD_2Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_high_price_usd_2y', projectSlug, from, to)
}
/**
* Gets the daily high price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_HIGH_PRICE_USD_3Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_high_price_usd_3y', projectSlug, from, to)
}
/**
* Gets the daily high price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_HIGH_PRICE_USD_5Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_high_price_usd_5y', projectSlug, from, to)
}
/**
* Gets the daily high price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_HIGH_PRICE_USD_10Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_high_price_usd_10y', projectSlug, from, to)
}
/**
* Gets the daily low price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_LOW_PRICE_USD (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_low_price_usd', projectSlug, from, to)
}
/**
* Gets the daily low price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_LOW_PRICE_USD_1D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_low_price_usd_1d', projectSlug, from, to)
}
/**
* Gets the daily low price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_LOW_PRICE_USD_7D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_low_price_usd_7d', projectSlug, from, to)
}
/**
* Gets the daily low price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_LOW_PRICE_USD_30D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_low_price_usd_30d', projectSlug, from, to)
}
/**
* Gets the daily low price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_LOW_PRICE_USD_60D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_low_price_usd_60d', projectSlug, from, to)
}
/**
* Gets the daily low price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_LOW_PRICE_USD_90D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_low_price_usd_90d', projectSlug, from, to)
}
/**
* Gets the daily low price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_LOW_PRICE_USD_180D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_low_price_usd_180d', projectSlug, from, to)
}
/**
* Gets the daily low price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_LOW_PRICE_USD_365D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_low_price_usd_365d', projectSlug, from, to)
}
/**
* Gets the daily low price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_LOW_PRICE_USD_2Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_low_price_usd_2y', projectSlug, from, to)
}
/**
* Gets the daily low price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_LOW_PRICE_USD_3Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_low_price_usd_3y', projectSlug, from, to)
}
/**
* Gets the daily low price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_LOW_PRICE_USD_5Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_low_price_usd_5y', projectSlug, from, to)
}
/**
* Gets the daily low price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_LOW_PRICE_USD_10Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_low_price_usd_10y', projectSlug, from, to)
}
/**
* Gets the daily opening price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_OPENING_PRICE_USD (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_opening_price_usd', projectSlug, from, to)
}
/**
* Gets the daily opening price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_OPENING_PRICE_USD_1D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_opening_price_usd_1d', projectSlug, from, to)
}
/**
* Gets the daily opening price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_OPENING_PRICE_USD_7D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_opening_price_usd_7d', projectSlug, from, to)
}
/**
* Gets the daily opening price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_OPENING_PRICE_USD_30D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_opening_price_usd_30d', projectSlug, from, to)
}
/**
* Gets the daily opening price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_OPENING_PRICE_USD_60D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_opening_price_usd_60d', projectSlug, from, to)
}
/**
* Gets the daily opening price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_OPENING_PRICE_USD_90D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_opening_price_usd_90d', projectSlug, from, to)
}
/**
* Gets the daily opening price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_OPENING_PRICE_USD_180D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_opening_price_usd_180d', projectSlug, from, to)
}
/**
* Gets the daily opening price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_OPENING_PRICE_USD_365D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_opening_price_usd_365d', projectSlug, from, to)
}
/**
* Gets the daily opening price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_OPENING_PRICE_USD_2Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_opening_price_usd_2y', projectSlug, from, to)
}
/**
* Gets the daily opening price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_OPENING_PRICE_USD_3Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_opening_price_usd_3y', projectSlug, from, to)
}
/**
* Gets the daily opening price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_OPENING_PRICE_USD_5Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_opening_price_usd_5y', projectSlug, from, to)
}
/**
* Gets the daily opening price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_OPENING_PRICE_USD_10Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_opening_price_usd_10y', projectSlug, from, to)
}
/**
* Gets the daily trading volume
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_TRADING_VOLUME_USD (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_trading_volume_usd', projectSlug, from, to)
}
/**
* Gets the daily trading volume
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_TRADING_VOLUME_USD_1D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_trading_volume_usd_1d', projectSlug, from, to)
}
/**
* Gets the daily trading volume
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_TRADING_VOLUME_USD_7D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_trading_volume_usd_7d', projectSlug, from, to)
}
/**
* Gets the daily trading volume
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_TRADING_VOLUME_USD_30D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_trading_volume_usd_30d', projectSlug, from, to)
}
/**
* Gets the daily trading volume
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_TRADING_VOLUME_USD_60D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_trading_volume_usd_60d', projectSlug, from, to)
}
/**
* Gets the daily trading volume
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_TRADING_VOLUME_USD_90D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_trading_volume_usd_90d', projectSlug, from, to)
}
/**
* Gets the daily trading volume
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_TRADING_VOLUME_USD_180D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_trading_volume_usd_180d', projectSlug, from, to)
}
/**
* Gets the daily trading volume
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_TRADING_VOLUME_USD_365D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_trading_volume_usd_365d', projectSlug, from, to)
}
/**
* Gets the daily trading volume
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_TRADING_VOLUME_USD_2Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_trading_volume_usd_2y', projectSlug, from, to)
}
/**
* Gets the daily trading volume
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_TRADING_VOLUME_USD_3Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_trading_volume_usd_3y', projectSlug, from, to)
}
/**
* Gets the daily trading volume
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_TRADING_VOLUME_USD_5Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_trading_volume_usd_5y', projectSlug, from, to)
}
/**
* Gets the daily trading volume
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_TRADING_VOLUME_USD_10Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_trading_volume_usd_10y', projectSlug, from, to)
}
/**
* Gets the daily active addresses
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_ACTIVE_ADDRESSES (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_active_addresses', projectSlug, from, to)
}
/**
* Gets the daily active addresses
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_ACTIVE_ADDRESSES_1D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_active_addresses_1d', projectSlug, from, to)
}
/**
* Gets the daily active addresses
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_ACTIVE_ADDRESSES_7D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_active_addresses_7d', projectSlug, from, to)
}
/**
* Gets the daily active addresses
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_ACTIVE_ADDRESSES_30D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_active_addresses_30d', projectSlug, from, to)
}
/**
* Gets the daily active addresses
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_ACTIVE_ADDRESSES_60D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_active_addresses_60d', projectSlug, from, to)
}
/**
* Gets the daily active addresses
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_ACTIVE_ADDRESSES_90D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_active_addresses_90d', projectSlug, from, to)
}
/**
* Gets the daily active addresses
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_ACTIVE_ADDRESSES_180D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_active_addresses_180d', projectSlug, from, to)
}
/**
* Gets the daily active addresses
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_ACTIVE_ADDRESSES_365D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_active_addresses_365d', projectSlug, from, to)
}
/**
* Gets the daily active addresses
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_ACTIVE_ADDRESSES_2Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_active_addresses_2y', projectSlug, from, to)
}
/**
* Gets the daily active addresses
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_ACTIVE_ADDRESSES_3Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_active_addresses_3y', projectSlug, from, to)
}
/**
* Gets the daily active addresses
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_ACTIVE_ADDRESSES_5Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_active_addresses_5y', projectSlug, from, to)
}
/**
* Gets the daily active addresses
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_DAILY_ACTIVE_ADDRESSES_10Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('daily_active_addresses_10y', projectSlug, from, to)
}
/**
* Gets the mean realised price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MEAN_REALIZED_PRICE_USD (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mean_realized_price_usd', projectSlug, from, to)
}
/**
* Gets the mean realised price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MEAN_REALIZED_PRICE_USD_1D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mean_realized_price_usd_1d', projectSlug, from, to)
}
/**
* Gets the mean realised price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MEAN_REALIZED_PRICE_USD_7D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mean_realized_price_usd_7d', projectSlug, from, to)
}
/**
* Gets the mean realised price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MEAN_REALIZED_PRICE_USD_30D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mean_realized_price_usd_30d', projectSlug, from, to)
}
/**
* Gets the mean realised price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MEAN_REALIZED_PRICE_USD_60D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mean_realized_price_usd_60d', projectSlug, from, to)
}
/**
* Gets the mean realised price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MEAN_REALIZED_PRICE_USD_90D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mean_realized_price_usd_90d', projectSlug, from, to)
}
/**
* Gets the mean realised price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MEAN_REALIZED_PRICE_USD_180D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mean_realized_price_usd_180d', projectSlug, from, to)
}
/**
* Gets the mean realised price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MEAN_REALIZED_PRICE_USD_365D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mean_realized_price_usd_365d', projectSlug, from, to)
}
/**
* Gets the mean realised price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MEAN_REALIZED_PRICE_USD_2Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mean_realized_price_usd_2y', projectSlug, from, to)
}
/**
* Gets the mean realised price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MEAN_REALIZED_PRICE_USD_3Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mean_realized_price_usd_3y', projectSlug, from, to)
}
/**
* Gets the mean realised price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MEAN_REALIZED_PRICE_USD_5Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mean_realized_price_usd_5y', projectSlug, from, to)
}
/**
* Gets the mean realised price
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MEAN_REALIZED_PRICE_USD_10Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mean_realized_price_usd_10y', projectSlug, from, to)
}
/**
* Gets the difference between MVRV
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MVRV_LONG_SHORT_DIFF (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mvrv_long_short_diff', projectSlug, from, to)
}
/**
* Gets the difference between MVRV
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MVRV_LONG_SHORT_DIFF_1D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mvrv_long_short_diff_1d', projectSlug, from, to)
}
/**
* Gets the difference between MVRV
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MVRV_LONG_SHORT_DIFF_7D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mvrv_long_short_diff_7d', projectSlug, from, to)
}
/**
* Gets the difference between MVRV
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MVRV_LONG_SHORT_DIFF_30D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mvrv_long_short_diff_30d', projectSlug, from, to)
}
/**
* Gets the difference between MVRV
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MVRV_LONG_SHORT_DIFF_60D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mvrv_long_short_diff_60d', projectSlug, from, to)
}
/**
* Gets the difference between MVRV
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MVRV_LONG_SHORT_DIFF_90D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mvrv_long_short_diff_90d', projectSlug, from, to)
}
/**
* Gets the difference between MVRV
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MVRV_LONG_SHORT_DIFF_180D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mvrv_long_short_diff_180d', projectSlug, from, to)
}
/**
* Gets the difference between MVRV
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MVRV_LONG_SHORT_DIFF_365D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mvrv_long_short_diff_365d', projectSlug, from, to)
}
/**
* Gets the difference between MVRV
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MVRV_LONG_SHORT_DIFF_2Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mvrv_long_short_diff_2y', projectSlug, from, to)
}
/**
* Gets the difference between MVRV
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MVRV_LONG_SHORT_DIFF_3Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mvrv_long_short_diff_3y', projectSlug, from, to)
}
/**
* Gets the difference between MVRV
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MVRV_LONG_SHORT_DIFF_5Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mvrv_long_short_diff_5y', projectSlug, from, to)
}
/**
* Gets the difference between MVRV
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MVRV_LONG_SHORT_DIFF_10Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mvrv_long_short_diff_10y', projectSlug, from, to)
}
/**
* Gets the MVRV
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MVRV_USD (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mvrv_usd', projectSlug, from, to)
}
/**
* Gets the MVRV
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MVRV_USD_1D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mvrv_usd_1d', projectSlug, from, to)
}
/**
* Gets the MVRV
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MVRV_USD_7D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mvrv_usd_7d', projectSlug, from, to)
}
/**
* Gets the MVRV
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MVRV_USD_30D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mvrv_usd_30d', projectSlug, from, to)
}
/**
* Gets the MVRV
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MVRV_USD_60D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mvrv_usd_60d', projectSlug, from, to)
}
/**
* Gets the MVRV
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MVRV_USD_90D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mvrv_usd_90d', projectSlug, from, to)
}
/**
* Gets the MVRV
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MVRV_USD_180D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mvrv_usd_180d', projectSlug, from, to)
}
/**
* Gets the MVRV
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MVRV_USD_365D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mvrv_usd_365d', projectSlug, from, to)
}
/**
* Gets the MVRV
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MVRV_USD_2Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mvrv_usd_2y', projectSlug, from, to)
}
/**
* Gets the MVRV
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MVRV_USD_3Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mvrv_usd_3y', projectSlug, from, to)
}
/**
* Gets the MVRV
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MVRV_USD_5Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mvrv_usd_5y', projectSlug, from, to)
}
/**
* Gets the MVRV
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MVRV_USD_10Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mvrv_usd_10y', projectSlug, from, to)
}
/**
* Gets the token circulation
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_CIRCULATION (projectSlug, from, to) {
  return handleErrors_(getMetric_)('circulation', projectSlug, from, to)
}
/**
* Gets the token circulation
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_CIRCULATION_1D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('circulation_1d', projectSlug, from, to)
}
/**
* Gets the token circulation
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_CIRCULATION_7D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('circulation_7d', projectSlug, from, to)
}
/**
* Gets the token circulation
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_CIRCULATION_30D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('circulation_30d', projectSlug, from, to)
}
/**
* Gets the token circulation
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_CIRCULATION_60D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('circulation_60d', projectSlug, from, to)
}
/**
* Gets the token circulation
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_CIRCULATION_90D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('circulation_90d', projectSlug, from, to)
}
/**
* Gets the token circulation
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_CIRCULATION_180D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('circulation_180d', projectSlug, from, to)
}
/**
* Gets the token circulation
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_CIRCULATION_365D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('circulation_365d', projectSlug, from, to)
}
/**
* Gets the token circulation
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_CIRCULATION_2Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('circulation_2y', projectSlug, from, to)
}
/**
* Gets the token circulation
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_CIRCULATION_3Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('circulation_3y', projectSlug, from, to)
}
/**
* Gets the token circulation
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_CIRCULATION_5Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('circulation_5y', projectSlug, from, to)
}
/**
* Gets the token circulation
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_CIRCULATION_10Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('circulation_10y', projectSlug, from, to)
}
/**
* Gets the token's mean age
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MEAN_AGE (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mean_age', projectSlug, from, to)
}
/**
* Gets the token's mean age
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MEAN_AGE_1D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mean_age_1d', projectSlug, from, to)
}
/**
* Gets the token's mean age
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MEAN_AGE_7D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mean_age_7d', projectSlug, from, to)
}
/**
* Gets the token's mean age
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MEAN_AGE_30D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mean_age_30d', projectSlug, from, to)
}
/**
* Gets the token's mean age
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MEAN_AGE_60D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mean_age_60d', projectSlug, from, to)
}
/**
* Gets the token's mean age
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MEAN_AGE_90D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mean_age_90d', projectSlug, from, to)
}
/**
* Gets the token's mean age
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MEAN_AGE_180D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mean_age_180d', projectSlug, from, to)
}
/**
* Gets the token's mean age
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MEAN_AGE_365D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mean_age_365d', projectSlug, from, to)
}
/**
* Gets the token's mean age
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MEAN_AGE_2Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mean_age_2y', projectSlug, from, to)
}
/**
* Gets the token's mean age
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MEAN_AGE_3Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mean_age_3y', projectSlug, from, to)
}
/**
* Gets the token's mean age
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MEAN_AGE_5Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mean_age_5y', projectSlug, from, to)
}
/**
* Gets the token's mean age
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_MEAN_AGE_10Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('mean_age_10y', projectSlug, from, to)
}
/**
* Gets a token's realised value
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_REALIZED_VALUE_USD (projectSlug, from, to) {
  return handleErrors_(getMetric_)('realized_value_usd', projectSlug, from, to)
}
/**
* Gets a token's realised value
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_REALIZED_VALUE_USD_1D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('realized_value_usd_1d', projectSlug, from, to)
}
/**
* Gets a token's realised value
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_REALIZED_VALUE_USD_7D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('realized_value_usd_7d', projectSlug, from, to)
}
/**
* Gets a token's realised value
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_REALIZED_VALUE_USD_30D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('realized_value_usd_30d', projectSlug, from, to)
}
/**
* Gets a token's realised value
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_REALIZED_VALUE_USD_60D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('realized_value_usd_60d', projectSlug, from, to)
}
/**
* Gets a token's realised value
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_REALIZED_VALUE_USD_90D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('realized_value_usd_90d', projectSlug, from, to)
}
/**
* Gets a token's realised value
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_REALIZED_VALUE_USD_180D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('realized_value_usd_180d', projectSlug, from, to)
}
/**
* Gets a token's realised value
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_REALIZED_VALUE_USD_365D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('realized_value_usd_365d', projectSlug, from, to)
}
/**
* Gets a token's realised value
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_REALIZED_VALUE_USD_2Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('realized_value_usd_2y', projectSlug, from, to)
}
/**
* Gets a token's realised value
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_REALIZED_VALUE_USD_3Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('realized_value_usd_3y', projectSlug, from, to)
}
/**
* Gets a token's realised value
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_REALIZED_VALUE_USD_5Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('realized_value_usd_5y', projectSlug, from, to)
}
/**
* Gets a token's realised value
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_REALIZED_VALUE_USD_10Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('realized_value_usd_10y', projectSlug, from, to)
}
/**
* Gets the token's velocity
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_VELOCITY (projectSlug, from, to) {
  return handleErrors_(getMetric_)('velocity', projectSlug, from, to)
}
/**
* Gets the token's velocity
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_VELOCITY_1D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('velocity_1d', projectSlug, from, to)
}
/**
* Gets the token's velocity
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_VELOCITY_7D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('velocity_7d', projectSlug, from, to)
}
/**
* Gets the token's velocity
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_VELOCITY_30D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('velocity_30d', projectSlug, from, to)
}
/**
* Gets the token's velocity
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_VELOCITY_60D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('velocity_60d', projectSlug, from, to)
}
/**
* Gets the token's velocity
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_VELOCITY_90D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('velocity_90d', projectSlug, from, to)
}
/**
* Gets the token's velocity
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_VELOCITY_180D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('velocity_180d', projectSlug, from, to)
}
/**
* Gets the token's velocity
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_VELOCITY_365D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('velocity_365d', projectSlug, from, to)
}
/**
* Gets the token's velocity
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_VELOCITY_2Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('velocity_2y', projectSlug, from, to)
}
/**
* Gets the token's velocity
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_VELOCITY_3Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('velocity_3y', projectSlug, from, to)
}
/**
* Gets the token's velocity
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_VELOCITY_5Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('velocity_5y', projectSlug, from, to)
}
/**
* Gets the token's velocity
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_VELOCITY_10Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('velocity_10y', projectSlug, from, to)
}
/**
* Gets the transaction volume
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_TRANSACTION_VOLUME (projectSlug, from, to) {
  return handleErrors_(getMetric_)('transaction_volume', projectSlug, from, to)
}
/**
* Gets the transaction volume
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_TRANSACTION_VOLUME_1D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('transaction_volume_1d', projectSlug, from, to)
}
/**
* Gets the transaction volume
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_TRANSACTION_VOLUME_7D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('transaction_volume_7d', projectSlug, from, to)
}
/**
* Gets the transaction volume
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_TRANSACTION_VOLUME_30D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('transaction_volume_30d', projectSlug, from, to)
}
/**
* Gets the transaction volume
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_TRANSACTION_VOLUME_60D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('transaction_volume_60d', projectSlug, from, to)
}
/**
* Gets the transaction volume
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_TRANSACTION_VOLUME_90D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('transaction_volume_90d', projectSlug, from, to)
}
/**
* Gets the transaction volume
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_TRANSACTION_VOLUME_180D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('transaction_volume_180d', projectSlug, from, to)
}
/**
* Gets the transaction volume
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_TRANSACTION_VOLUME_365D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('transaction_volume_365d', projectSlug, from, to)
}
/**
* Gets the transaction volume
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_TRANSACTION_VOLUME_2Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('transaction_volume_2y', projectSlug, from, to)
}
/**
* Gets the transaction volume
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_TRANSACTION_VOLUME_3Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('transaction_volume_3y', projectSlug, from, to)
}
/**
* Gets the transaction volume
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_TRANSACTION_VOLUME_5Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('transaction_volume_5y', projectSlug, from, to)
}
/**
* Gets the transaction volume
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_TRANSACTION_VOLUME_10Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('transaction_volume_10y', projectSlug, from, to)
}
/**
* Gets the transaction volume for every 5 minutes
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_TRANSACTION_VOLUME_5MIN (projectSlug, from, to) {
  return handleErrors_(getMetric_)('transaction_volume_5min', projectSlug, from, to)
}
/**
* Gets the transaction volume for every 5 minutes
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_TRANSACTION_VOLUME_5MIN_1D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('transaction_volume_5min_1d', projectSlug, from, to)
}
/**
* Gets the transaction volume for every 5 minutes
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_TRANSACTION_VOLUME_5MIN_7D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('transaction_volume_5min_7d', projectSlug, from, to)
}
/**
* Gets the transaction volume for every 5 minutes
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_TRANSACTION_VOLUME_5MIN_30D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('transaction_volume_5min_30d', projectSlug, from, to)
}
/**
* Gets the transaction volume for every 5 minutes
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_TRANSACTION_VOLUME_5MIN_60D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('transaction_volume_5min_60d', projectSlug, from, to)
}
/**
* Gets the transaction volume for every 5 minutes
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_TRANSACTION_VOLUME_5MIN_90D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('transaction_volume_5min_90d', projectSlug, from, to)
}
/**
* Gets the transaction volume for every 5 minutes
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_TRANSACTION_VOLUME_5MIN_180D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('transaction_volume_5min_180d', projectSlug, from, to)
}
/**
* Gets the transaction volume for every 5 minutes
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_TRANSACTION_VOLUME_5MIN_365D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('transaction_volume_5min_365d', projectSlug, from, to)
}
/**
* Gets the transaction volume for every 5 minutes
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_TRANSACTION_VOLUME_5MIN_2Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('transaction_volume_5min_2y', projectSlug, from, to)
}
/**
* Gets the transaction volume for every 5 minutes
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_TRANSACTION_VOLUME_5MIN_3Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('transaction_volume_5min_3y', projectSlug, from, to)
}
/**
* Gets the transaction volume for every 5 minutes
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_TRANSACTION_VOLUME_5MIN_5Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('transaction_volume_5min_5y', projectSlug, from, to)
}
/**
* Gets the transaction volume for every 5 minutes
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_TRANSACTION_VOLUME_5MIN_10Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('transaction_volume_5min_10y', projectSlug, from, to)
}
/**
* Gets the exchange inflow
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_INFLOW (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_inflow', projectSlug, from, to)
}
/**
* Gets the exchange inflow
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_INFLOW_1D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_inflow_1d', projectSlug, from, to)
}
/**
* Gets the exchange inflow
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_INFLOW_7D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_inflow_7d', projectSlug, from, to)
}
/**
* Gets the exchange inflow
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_INFLOW_30D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_inflow_30d', projectSlug, from, to)
}
/**
* Gets the exchange inflow
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_INFLOW_60D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_inflow_60d', projectSlug, from, to)
}
/**
* Gets the exchange inflow
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_INFLOW_90D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_inflow_90d', projectSlug, from, to)
}
/**
* Gets the exchange inflow
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_INFLOW_180D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_inflow_180d', projectSlug, from, to)
}
/**
* Gets the exchange inflow
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_INFLOW_365D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_inflow_365d', projectSlug, from, to)
}
/**
* Gets the exchange inflow
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_INFLOW_2Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_inflow_2y', projectSlug, from, to)
}
/**
* Gets the exchange inflow
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_INFLOW_3Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_inflow_3y', projectSlug, from, to)
}
/**
* Gets the exchange inflow
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_INFLOW_5Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_inflow_5y', projectSlug, from, to)
}
/**
* Gets the exchange inflow
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_INFLOW_10Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_inflow_10y', projectSlug, from, to)
}
/**
* Gets the exchange outflow
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_OUTFLOW (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_outflow', projectSlug, from, to)
}
/**
* Gets the exchange outflow
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_OUTFLOW_1D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_outflow_1d', projectSlug, from, to)
}
/**
* Gets the exchange outflow
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_OUTFLOW_7D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_outflow_7d', projectSlug, from, to)
}
/**
* Gets the exchange outflow
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_OUTFLOW_30D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_outflow_30d', projectSlug, from, to)
}
/**
* Gets the exchange outflow
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_OUTFLOW_60D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_outflow_60d', projectSlug, from, to)
}
/**
* Gets the exchange outflow
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_OUTFLOW_90D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_outflow_90d', projectSlug, from, to)
}
/**
* Gets the exchange outflow
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_OUTFLOW_180D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_outflow_180d', projectSlug, from, to)
}
/**
* Gets the exchange outflow
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_OUTFLOW_365D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_outflow_365d', projectSlug, from, to)
}
/**
* Gets the exchange outflow
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_OUTFLOW_2Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_outflow_2y', projectSlug, from, to)
}
/**
* Gets the exchange outflow
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_OUTFLOW_3Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_outflow_3y', projectSlug, from, to)
}
/**
* Gets the exchange outflow
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_OUTFLOW_5Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_outflow_5y', projectSlug, from, to)
}
/**
* Gets the exchange outflow
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_OUTFLOW_10Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_outflow_10y', projectSlug, from, to)
}
/**
* Gets the exchange balance
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_BALANCE (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_balance', projectSlug, from, to)
}
/**
* Gets the exchange balance
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_BALANCE_1D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_balance_1d', projectSlug, from, to)
}
/**
* Gets the exchange balance
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_BALANCE_7D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_balance_7d', projectSlug, from, to)
}
/**
* Gets the exchange balance
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_BALANCE_30D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_balance_30d', projectSlug, from, to)
}
/**
* Gets the exchange balance
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_BALANCE_60D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_balance_60d', projectSlug, from, to)
}
/**
* Gets the exchange balance
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_BALANCE_90D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_balance_90d', projectSlug, from, to)
}
/**
* Gets the exchange balance
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_BALANCE_180D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_balance_180d', projectSlug, from, to)
}
/**
* Gets the exchange balance
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_BALANCE_365D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_balance_365d', projectSlug, from, to)
}
/**
* Gets the exchange balance
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_BALANCE_2Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_balance_2y', projectSlug, from, to)
}
/**
* Gets the exchange balance
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_BALANCE_3Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_balance_3y', projectSlug, from, to)
}
/**
* Gets the exchange balance
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_BALANCE_5Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_balance_5y', projectSlug, from, to)
}
/**
* Gets the exchange balance
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_EXCHANGE_BALANCE_10Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('exchange_balance_10y', projectSlug, from, to)
}
/**
* Gets the token's age destroyed
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_AGE_DESTROYED (projectSlug, from, to) {
  return handleErrors_(getMetric_)('age_destroyed', projectSlug, from, to)
}
/**
* Gets the token's age destroyed
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_AGE_DESTROYED_1D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('age_destroyed_1d', projectSlug, from, to)
}
/**
* Gets the token's age destroyed
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_AGE_DESTROYED_7D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('age_destroyed_7d', projectSlug, from, to)
}
/**
* Gets the token's age destroyed
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_AGE_DESTROYED_30D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('age_destroyed_30d', projectSlug, from, to)
}
/**
* Gets the token's age destroyed
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_AGE_DESTROYED_60D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('age_destroyed_60d', projectSlug, from, to)
}
/**
* Gets the token's age destroyed
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_AGE_DESTROYED_90D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('age_destroyed_90d', projectSlug, from, to)
}
/**
* Gets the token's age destroyed
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_AGE_DESTROYED_180D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('age_destroyed_180d', projectSlug, from, to)
}
/**
* Gets the token's age destroyed
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_AGE_DESTROYED_365D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('age_destroyed_365d', projectSlug, from, to)
}
/**
* Gets the token's age destroyed
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_AGE_DESTROYED_2Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('age_destroyed_2y', projectSlug, from, to)
}
/**
* Gets the token's age destroyed
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_AGE_DESTROYED_3Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('age_destroyed_3y', projectSlug, from, to)
}
/**
* Gets the token's age destroyed
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_AGE_DESTROYED_5Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('age_destroyed_5y', projectSlug, from, to)
}
/**
* Gets the token's age destroyed
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_AGE_DESTROYED_10Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('age_destroyed_10y', projectSlug, from, to)
}
/**
* Gets the token's age destroyed for every 5 minutes
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_AGE_DESTROYED_5MIN (projectSlug, from, to) {
  return handleErrors_(getMetric_)('age_destroyed_5min', projectSlug, from, to)
}
/**
* Gets the token's age destroyed for every 5 minutes
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_AGE_DESTROYED_5MIN_1D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('age_destroyed_5min_1d', projectSlug, from, to)
}
/**
* Gets the token's age destroyed for every 5 minutes
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_AGE_DESTROYED_5MIN_7D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('age_destroyed_5min_7d', projectSlug, from, to)
}
/**
* Gets the token's age destroyed for every 5 minutes
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_AGE_DESTROYED_5MIN_30D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('age_destroyed_5min_30d', projectSlug, from, to)
}
/**
* Gets the token's age destroyed for every 5 minutes
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_AGE_DESTROYED_5MIN_60D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('age_destroyed_5min_60d', projectSlug, from, to)
}
/**
* Gets the token's age destroyed for every 5 minutes
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_AGE_DESTROYED_5MIN_90D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('age_destroyed_5min_90d', projectSlug, from, to)
}
/**
* Gets the token's age destroyed for every 5 minutes
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_AGE_DESTROYED_5MIN_180D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('age_destroyed_5min_180d', projectSlug, from, to)
}
/**
* Gets the token's age destroyed for every 5 minutes
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_AGE_DESTROYED_5MIN_365D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('age_destroyed_5min_365d', projectSlug, from, to)
}
/**
* Gets the token's age destroyed for every 5 minutes
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_AGE_DESTROYED_5MIN_2Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('age_destroyed_5min_2y', projectSlug, from, to)
}
/**
* Gets the token's age destroyed for every 5 minutes
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_AGE_DESTROYED_5MIN_3Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('age_destroyed_5min_3y', projectSlug, from, to)
}
/**
* Gets the token's age destroyed for every 5 minutes
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_AGE_DESTROYED_5MIN_5Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('age_destroyed_5min_5y', projectSlug, from, to)
}
/**
* Gets the token's age destroyed for every 5 minutes
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_AGE_DESTROYED_5MIN_10Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('age_destroyed_5min_10y', projectSlug, from, to)
}
/**
* Gets the Network Value to Transaction ratio
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_NVT (projectSlug, from, to) {
  return handleErrors_(getMetric_)('nvt', projectSlug, from, to)
}
/**
* Gets the Network Value to Transaction ratio
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_NVT_1D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('nvt_1d', projectSlug, from, to)
}
/**
* Gets the Network Value to Transaction ratio
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_NVT_7D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('nvt_7d', projectSlug, from, to)
}
/**
* Gets the Network Value to Transaction ratio
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_NVT_30D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('nvt_30d', projectSlug, from, to)
}
/**
* Gets the Network Value to Transaction ratio
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_NVT_60D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('nvt_60d', projectSlug, from, to)
}
/**
* Gets the Network Value to Transaction ratio
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_NVT_90D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('nvt_90d', projectSlug, from, to)
}
/**
* Gets the Network Value to Transaction ratio
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_NVT_180D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('nvt_180d', projectSlug, from, to)
}
/**
* Gets the Network Value to Transaction ratio
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_NVT_365D (projectSlug, from, to) {
  return handleErrors_(getMetric_)('nvt_365d', projectSlug, from, to)
}
/**
* Gets the Network Value to Transaction ratio
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_NVT_2Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('nvt_2y', projectSlug, from, to)
}
/**
* Gets the Network Value to Transaction ratio
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_NVT_3Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('nvt_3y', projectSlug, from, to)
}
/**
* Gets the Network Value to Transaction ratio
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_NVT_5Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('nvt_5y', projectSlug, from, to)
}
/**
* Gets the Network Value to Transaction ratio
* @param {string} projectSlug Name of the asset at sanbase,
* which can be found at the end of the URL (eg. the Santiment URL is
* https://app.santiment.net/projects/santiment, so the projectSlug would be santiment).
* @param {date} from The starting date to fetch the data. Example: DATE(2018, 9, 20)
* @param {date} to The ending date to fetch the data. Example: DATE(2018, 9, 21)
* @returns {number} absolute price change.
* @customfunction
*/
function SAN_NVT_10Y (projectSlug, from, to) {
  return handleErrors_(getMetric_)('nvt_10y', projectSlug, from, to)
}
